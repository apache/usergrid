<?xml version="1.0" encoding="UTF-8"?>
<job-scheduling-data
	xmlns="http://www.quartz-scheduler.org/xml/JobSchedulingData"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.quartz-scheduler.org/xml/JobSchedulingData http://www.quartz-scheduler.org/xml/job_scheduling_data_1_8.xsd"
	version="1.8">

	<pre-processing-commands>
		<delete-jobs-in-group>*</delete-jobs-in-group>  <!-- clear all jobs in scheduler -->
		<delete-triggers-in-group>*</delete-triggers-in-group> <!-- clear all triggers in scheduler -->
	</pre-processing-commands>

	<processing-directives>
		<!--
			if there are any jobs/trigger in scheduler of same name, overwrite
			them
		-->
		<overwrite-existing-data>true</overwrite-existing-data>
		<!--
			if there are any jobs/trigger in scheduler of same name,ignore them
			rather then generating an error
		-->
		<ignore-duplicates>false</ignore-duplicates>
	</processing-directives>

	<schedule>
		<job>
			<name>Sqs2RdsJob</name>			
			<job-class>org.apache.usergrid.apm.service.MetricsInjestionSchedulerJob</job-class>
			<!--
				<job-data-map> <entry> <key>Name of SQS queue</key>
				<value>q1,q2,q3</value> </entry> </job-data-map> </job-data-map>
			-->
		</job>
		<!-- It's not being used currently -->
		<trigger>
			<simple>
				<name>TriggerJustOnceWhenServerStarts</name>
				<group>ideawheel</group>				
				<job-name>Sqs2RdsJob</job-name>
				<repeat-count>0</repeat-count> 
				<repeat-interval>0</repeat-interval>
			</simple>
		</trigger>
	</schedule>
</job-scheduling-data>
	    
